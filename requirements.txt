# ==========================================
# Основные библиотеки для работы с VK и HTTP
# ==========================================
vk-api==11.10.0
requests==2.32.3
httpx==0.27.2          # Используется для асинхронных/быстрых запросов к Yandex GPT

# ==========================================
# Конфигурация и окружение
# ==========================================
python-dotenv==1.0.1   # Для загрузки переменных из файла .env

# ==========================================
# База данных (для мульти-пользовательских токенов)
# ==========================================
SQLAlchemy==2.0.36     # ORM для работы с SQLite (users.db)

# ==========================================
# Обработка изображений (включая HEIC/HEIF с iPhone)
# ==========================================
Pillow==10.4.0         # Базовая библиотека для работы с изображениями
pillow-heif==0.21.0    # Плагин для конвертации HEIC/HEIF в JPG (критически важно!)

# ==========================================
# Дополнительно (если используется в llm_service)
# ==========================================
openai==1.58.1         # Оставьте, если в llm_service_yandex.py есть вызовы OpenAI-совместимого API